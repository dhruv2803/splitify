# Project Rules

The following rules must be followed by any agent working on this project:

- **Documentation**: Always update the `docs/` folder along with making changes in the project. Ensure architecture, data models, and feature lists stay in sync with the codebase.
- **Verification**: Always verify all changes before completion. Check for any dependencies or regressions introduced by the changes.
